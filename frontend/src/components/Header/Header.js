// @flow
import React from 'react'
import s from './Header.module.css'

const Header = () => (
  <header className={s.container}>
    <h1 className={s.title}>Sense Well Dashboard</h1>
  </header>
)

export default Header